import React from 'react'

import Layout from '../components/layout'

const SecondPage = () => (
  <Layout>
    <h1>Contact us</h1>
    <p>This is a bunch of blocks that give a few bits of information about each on of the UnitedCode developers and it can be clicked to open into their profiles and personal projects</p>
  </Layout>
)

export default SecondPage
